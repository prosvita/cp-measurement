# Backend DataSource для відображення статистики

## Розробка

```sh
# Встановлення залежностей
npm i

# Запуск
npm run dev
```

## Конфігурація

Налаштовується змінюються в конфігураційних файлах `/configs/*/node.js` в залежності від змінної оточення `NODE_ENV`.
